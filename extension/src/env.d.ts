/** Injected by Vite `define` (scripts/build.ts) and by vitest.config.ts for unit tests. */
declare const __DEFAULT_SERVER_ORIGIN__: string;
/** Which manifest this bundle was built for; host patterns differ per browser (lib/origin.ts). */
declare const __BROWSER_TARGET__: 'chrome' | 'firefox';
